---
type: daily
tags: [diario, hermes]
title: Diário 2026-06-24-20260624
description: Daily note gerada pelo wiki_review
timestamp: 2026-06-24T16:45:04+00:00
status: draft
---


## Verificação proativa de sistemas — 13:44

Giovani pediu confirmação de que a wiki está commitada e depois quis verificar se o wiki_review está realmente funcionando — não aceita "achar que funciona", quer evidência concreta (logs, contador, conteúdo do diário, timestamps dos commits). Mostra um perfil metódico: testar o sistema, não confiar na configuração.

## wiki_review confirmado operacional com evidências — 13:44

Investiguei o plugin wiki_review em 4 camadas: (1) arquivos do plugin em `~/.hermes/plugins/wiki-review/` com `__init__.py` e `plugin.yaml`, (2) config.yaml com `wiki-review` habilitado em `plugins.enabled`, (3) contador em `/root/.hermes/wiki_review_counter` = 1 (pós-disparo), (4) diários com conteúdo real — `2026-06-24-20260624.md` com 55 KB e 457 linhas de insights timestampados. Gateway rodando como processo system (pid 3294618), não Docker. Últimos 15 commits wiki_review entre 10:25 e 13:43 BRT.

## Pendência: wiki_review estável, seguir para próximos itens — 13:44

wiki_review funcional e sem issues. Próximos passos pendentes na wiki em [[pendencias/proximos-passos.md]] — limpeza do source tree do Hermes e print visual do wiki_review no terminal seguem como itens abertos.

## Teste do próprio wiki_review via trigger manual — 13:45

Giovani disparou o prompt do wiki_review manualmente duas vezes seguidas, confirmando que o mecanismo de revisão está acessível e funcional. O primeiro trigger gerou 3 seções de insight e foi reportado com confirmação. Isso sugere que ele está validando o funcionamento do sistema — não só o plugin automático, mas também a capacidade de invocação explícita.

## nudge_interval confirmado como 2, não 10 — 13:44
O usuário notou que o wiki_review estava disparando com frequência alta e questionou se o intervalo era 2 ou 10. Verificando em `memory.nudge_interval` no config.yaml: o valor era `2`. Isso explica os 7 commits em 15 minutos. O padrão no plugin é 10, mas o config.yaml tinha sobrescrito para 2.

## Preferência revelada: prefere saber os detalhes exatos — 13:44
Giovani questiona ativamente sempre que percebe comportamento inesperado (frequência de disparo do wiki_review). Não aceita "está funcionando" sem entender os parâmetros exatos — quer saber o *porquê* do comportamento, não apenas o *que* está acontecendo. Prefere respostas diretas com evidência (neste caso: linhas do config.yaml + código do plugin que lê o valor).

## Preferência revelada: precisão factual acima de conveniência — 13:47
Giovani chamou atenção pro fato de eu ter dito que o wiki_review disparava a cada 10 turnos baseado apenas no fallback do código (10) e na documentação, sem verificar o valor real no config.yaml (que é 2). Ele não queria que eu corrigisse — queria entender de onde saiu o número errado. Prefere que eu investigue a fundo em vez de assumir valores padrão. Valoriza precisão documentada contra estado real.

## Config descoberta: nudge_interval = 2 (não 10) — 13:47
O `memory.nudge_interval` no config.yaml está setado como `2`, não o fallback padrão de `10` que está no código do plugin. Isso explica os 7 commits de wiki_review em 15 minutos (13:28-13:43). O plugin lê corretamente o valor do config em runtime via `hermes_cli.config.load_config()` — a documentação na wiki estava desatualizada mostrando "10" como valor real.

## Pendência: documentação do wiki_review desatualizada — 13:47
A tabela de parâmetros em `wiki/automacao/wiki-review.md` mostra "10" como intervalo de disparo, mas o config.yaml real tem `nudge_interval: 2`. A documentação precisa refletir o valor configurado, não o fallback padrão.

## Correção: wiki_review nudge_interval — 16:47

O usuário perguntou se o wiki_review estava funcionando e eu afirmei que disparava a cada 10 turnos. Na verdade o config.yaml tem `nudge_interval: 2`. O erro veio de eu ler o fallback padrão no código (`__init__.py` retorna 10 se não achar no config) e a documentação da wiki (tabela diz 10) sem verificar o valor real em runtime. O usuário quis entender a *origem* do erro, não que eu consertasse imediatamente — estilo de aprendizado por tracing de causa raiz.

## Preferência revelada: entende o erro antes de agir — 16:47

Quando corrigi que o wiki_review disparava a cada 2 turnos e não 10, o usuário não pediu "muda pra X". Perguntou "por que vc disse 10? quero entender". Sinal claro de que prefere **rastrear a causa raiz** de um erro antes de tomar qualquer ação corretiva. Não é sobre o fix, é sobre o aprendizado.

## Correção registrada na memória: verificar config.yaml em runtime — 16:47

Eu mesmo salvei na `memory()` do Hermes a lição: "NUNCA afirmar valor documentado/código como real — verificar config.yaml em runtime". A memory estava quase cheia (2.176/2.200 chars) e precisei consolidar — removi 1 entrada e simplifiquei 2 outras pra caber a nova lição. Resultado: 1.893/2.200 chars, 11 entradas.
